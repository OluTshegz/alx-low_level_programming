#include "main.h"

/**
 * mul - multiplies two number values (integers/digits)
 * @a: integer to be multiplied with b
 * @b: integer to be multiplied with a
 * Return: results of a and b
 */

int mul(int a, int b)
{
	int i;

	i = a * b;

	return (i);
}
