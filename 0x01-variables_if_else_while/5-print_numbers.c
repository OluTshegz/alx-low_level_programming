#include <stdio.h>

/**
 * main - prints single digit numbers of base 10
 *
 * Return: 0 if successful
 */

int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
	{
		printf("%d", x);
	}
	printf("\n");

	return (0);
}
