#include <stdio.h>
/**
 * main - Entry point that prints a string of characters
 * Description: Program that prints a statement without using printf but instead uses puts
 * Return: 0 if successful
 */
int main(void)
{
	puts ("\"Programming is like building a multilingual puzzle\n");
	return (0);
}
